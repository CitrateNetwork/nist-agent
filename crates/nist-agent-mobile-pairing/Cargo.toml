[package]
name = "nist-agent-mobile-pairing"
publish = false  # internal workspace crate; never published to crates.io
description = "Mobile-companion pairing protocol, attestation allowlist, per-overlay eligibility + TTL, and wire formats per RFC §5.6. Render-model and rule layer consumed by both the daemon and the (deferred) native iOS / Android apps."
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
nist-agent-prelude = { path = "../nist-agent-prelude" }
citrate-agent-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
hex = { workspace = true }
zeroize = { workspace = true }
